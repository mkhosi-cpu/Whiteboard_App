variable "region" {
  description = "AWS region"
  type        = string
  default     = "us-east-1"
}

# This code defines the region variable that we referenced earlier. Change the default region to the one you want to use.